import { UrlSenitizePipe } from './url-senitize.pipe';

describe('UrlSenitizePipe', () => {
  it('create an instance', () => {
    const pipe = new UrlSenitizePipe();
    expect(pipe).toBeTruthy();
  });
});
